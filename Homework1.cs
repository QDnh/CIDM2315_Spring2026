namespace Homework1;

class Program
{
    static void Main(string[] args)
    {
        // Define variables and values.
        double X = 2.5;
        double Y = 3.3;
        double Z = (4*X*X + 3*Y);
        
        // Output values in desired format.
        Console.WriteLine($"X = {X}, Y = {Y}");
        Console.WriteLine($"The value of Z is: {Z}");
    }
}
